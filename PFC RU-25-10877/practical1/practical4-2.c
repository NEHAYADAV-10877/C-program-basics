#include <stdio.h>

int main(){
    int a =5, b = 25;
    int max = (a>b) ? a : b;   // ternary operator
    printf("Max of %d and %d is: %d\n", a, b, max);
    printf("Neha Yadav\n");

    return 0;

}
